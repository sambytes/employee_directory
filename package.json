{
  "name": "employee-directory",
  "version": "1.0.0",
  "description": "Employee directory for Postlight coding challenge",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "swagger-ui": "node ./server/app.js"
  },
  "author": "Sam Menza",
  "license": "ISC",
  "dependencies": {
    "swagger": "^0.7.5"
  }
}
